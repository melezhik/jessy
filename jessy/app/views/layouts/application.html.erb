<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Jessy" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Jessy" %>">
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
    <% if user_signed_in? %>
    <p class="breadcrumb">user: <strong><%= current_user.username %></strong><br>role: <strong><%= current_user.roles_mask == 1 ? 'admin' : 'user' %></strong></p>
    <% end %>
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>
  </body>
</html>
